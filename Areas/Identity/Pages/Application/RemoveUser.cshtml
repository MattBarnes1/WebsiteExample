@page
@inject UserManager<UserData> myIDs
@model RemoveUserModel
@{
}
<form method="post">
    @if (Model.myUserList != null && Model.myUserList.RemoveThem != null)
    {
        @for (int i = 0; i < Model.myUserList.RemoveThem.Length; i++)
        {
            <input asp-for="myUserList.RemoveThem[i]" type="checkbox" />
            <label asp-for="myUserList.myUser[i].UserName" />
            <label asp-for="myUserList.myUser[i].Id" /> //Todo: panel that has the delete on it. + Warning
                                                      
            <br />
        }
    }
</form>